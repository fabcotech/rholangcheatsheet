const a = {
  bgColor: "230,230,230",
  classes: "text-black",
  title: "Power Boxes",
  lines: [
    ["<pre>stdout(`rho:io:stdout`)</pre>", "stdout!( any_process ) \n prints to screen" ],
    ["<pre>sha256Hash(`rho:crypto:sha256Hash`)</pre>", " sha256Hash!( byte_array, *return) \n calculates sha256hash "],
  ]
}

export default a;
